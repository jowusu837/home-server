# Caddy Reverse Proxy Configuration
# Copy this file to .env.caddy and update the values

# Base domain for your services
# This is used in the Caddyfile for subdomain routing
# Example: homeserver.local, home.lan, myserver.local
BASE_DOMAIN=homeserver.local
